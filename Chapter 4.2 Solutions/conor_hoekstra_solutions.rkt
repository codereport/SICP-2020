;; Exercise 4.25 (543-4)

;; Applicative order will run forever - because base case will never be hit "alone" - both arguments will always be evaluated.
;; Normal order will not have this problem and will work fine.

